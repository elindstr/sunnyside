<h1>Bill for {{format_date bill.date}}</h1>

<div class='invoice-body'>
    <strong>Billed To:</strong><br>
    {{bill.customer.first_name}} {{bill.customer.last_name}}<br>
    {{bill.customer.address}}<br>
    {{bill.customer.email}}<br>
</div>

<div>Total amount due: {{bill.amount}}</div>
{{!-- if the bill is unpaid render the link to stripe to pay it --}}
{{#if bill.hasOutstandingAmount}}
<div><a href={{bill.stripe_payment_url}}>Make a payment</a></div>
{{/if}}